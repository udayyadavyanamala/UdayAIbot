# Copy this file to `.env` locally (do NOT commit `.env`)
OPENROUTER_API_KEY=your_openrouter_api_key_here
PORT=3001

# Example model definitions (keep names and format)
MODEL_1=openrouter/free|✨ Auto (Best Free)|Automatically picks the best free model
